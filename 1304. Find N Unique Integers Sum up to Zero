class Solution {
public:
    vector<int> sumZero(int n) {
        vector<int> ans(n, 0);
        for (int i = 0; i * 2 + 1 < n; i++) {
            ans[2 * i] = i + 1;
            ans[2 * i + 1] = -1 * (i + 1);
        }
        return ans;
    }
};
// 0ms 9.70MB